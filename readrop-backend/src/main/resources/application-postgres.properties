# PostgreSQL profile — used with docker-compose (db service) or any PG instance.
# Enable with: SPRING_PROFILES_ACTIVE=postgres

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:readrop}
spring.datasource.username=${DB_USER:readrop}
spring.datasource.password=${DB_PASSWORD:readrop}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# No H2 console outside the in-memory dev profile.
spring.h2.console.enabled=false

# OpenShift Route exposing the backend Service over HTTPS (edge termination).
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: skilloport-backend
  labels:
    app: skilloport-backend
spec:
  to:
    kind: Service
    name: skilloport-backend
  port:
    targetPort: 3000
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
